<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>

    <AssemblyName>Owid.Client.Test</AssemblyName>
    <Platforms>AnyCPU</Platforms>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <Nullable>enable</Nullable>

    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.6.0" />
    <PackageReference Include="MSTest.TestAdapter" Version="4.2.3" />
    <PackageReference Include="MSTest.TestFramework" Version="4.2.3" />
    <PackageReference Include="coverlet.collector" Version="10.0.1">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <!-- A genuine identifier and the key schedule its creator published,
         read by PublishedScheduleTests. Named explicitly so the test asks
         for the file name rather than a name MSBuild derives. -->
    <EmbeddedResource Include="Data\51did-published-keys.json">
      <LogicalName>51did-published-keys.json</LogicalName>
    </EmbeddedResource>
    <EmbeddedResource Include="Data\51did-published-identifier.json">
      <LogicalName>51did-published-identifier.json</LogicalName>
    </EmbeddedResource>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Owid.Client\Owid.Client.csproj" />
    <ProjectReference Include="..\Owid.Client.Controllers\Owid.Client.Controllers.csproj" />
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

</Project>
